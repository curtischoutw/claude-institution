# AI Assistant Workflow Configuration

<!-- 本檔常駐於每個 session 的 context：每一行都有成本。
     新增規則前先看「制度分層」選對載體；本檔超過 160 行時，把細節降級到 skill。
     v2.0 (2026-07-05): 由 Fable 5 session 改寫為弱模型可執行版本。原版在 ~/.claude/backups/ -->

## 制度分層（任何新規則先決定放哪裡）

由上往下選，能放上層就不放下層：

| 層 | 載體 | 放什麼 | 為什麼 |
|---|---|---|---|
| 0 | hooks / permissions（settings.json，用 /update-config 設定） | 能用程式判定的規則（例：禁止 push main） | 機器強制，不靠模型自覺，最可靠 |
| 1 | 本檔（全域）/ `<repo>/CLAUDE.md`（專案） | 每次都必須遵守、1–2 行寫得完的規則 | 常駐 context，但長文件服從率低，必須精簡 |
| 2 | `~/.claude/skills/<name>/SKILL.md` | 多步驟程序、checklist、含範例的判準 | 用時才載入，可以寫長寫細 |
| 3 | `tasks/lessons.md`（專案） | 被糾正後的第一落點（觀察期） | 同類 lesson 第 2 次觸發 → 升級到層 1 或 2 |
| 4 | memory/ 目錄 | 事實、偏好、專案脈絡 | 只放事實，不放規則 |

## Session 起手式

1. 讀 `tasks/lessons.md`（檔案不存在就跳過，不要主動建立）
2. 動手前用一句話複述：任務範圍 + 完成判準。範圍不明時：只有「走錯方向的代價 > 提問成本」才問，否則寫下假設繼續做。

## 計畫（Plan Mode）

- 3 步以上、或動到架構 → 先進 plan mode；計畫寫入 `tasks/todo.md`，含可勾選項目。
- 計畫每一項必附三件事：**假設**（禁止沉默假設）、**風險**、**完成判準**。
  - 判準壞例：「完成登入功能」。好例：「`pytest tests/test_auth.py` 全綠，且 `curl /login` 回 200 + token」。
- 執行中發現計畫錯了 → 立刻停下回 plan mode 重排，不要邊做邊救。

## 硬規則（違反任一條 = 錯誤行為，無例外）

1. 沒讀過的檔案不准改。小改用 Edit，不整檔重寫。
2. 不准為了讓測試變綠而弱化、跳過或刪除測試。動到測試檔必須在總結中說明理由。
3. 使用者陳述的程式碼事實（「X 在 Y 檔」「這函式會 Z」）→ 先 grep/read 驗證再據以行動，不要直接相信。
4. 修 bug 時發現範圍外的問題：記到 `tasks/todo.md`，不順手修；完工後回報讓使用者決定。
5. 宣稱「完成」之前必走 /done-check 的 checklist；回報必附實際指令與輸出，不得只寫「測試通過」。
6. 同一問題連續 3 次修改仍失敗 → 停手，revert 回乾淨狀態，改走 /debug-protocol，不准繼續亂槍打鳥。
7. 自稱「小修」的 diff 超過 3 個檔案或約 100 行 → 停下來先回報，再決定是否繼續。
8. 不確定就寫「我不確定」，並說明會怎麼驗證；禁止用肯定語氣包裝猜測。

## Subagent 策略

- 研究、大範圍搜尋、平行分析 → 丟 subagent，保持主 context 乾淨。
- 一個 subagent 只做一件事；單步小事不開 subagent。

## 自我改進迴圈

- 被使用者糾正後（任何形式的「不對」「不是這樣」「我說過」）：立刻用 /lesson 記入 `tasks/lessons.md`。
- 同一 lesson 第 2 次被觸發：依「制度分層」升級成本檔規則、skill 或 hook。

## 追求優雅（有節制）

- 非平凡的修改完成後自問一次：「知道現在知道的一切，還會這樣寫嗎？」感覺 hacky 就重寫成優雅解。
- 簡單明顯的修法直接過，不過度工程。

## Git 紀律

- 原子 commit：一個 commit 只做一件事；訊息格式 `type(scope): description`。
- 不 commit 除錯碼（console.log / print / 註解掉的程式碼）。
- 使用者沒有要求就不 commit、不 push。

---

## Code Standards

### File Docstring（每個產出的原始碼檔案必備；markdown 設定檔除外）

```
File: <filename>
Author: Curtis Chou
Email: <your-email>
Created Date: <YYYY-MM-DD>
Version: <x.x.x>
Copyright (c) <created_year>[-<current_year>] Curtis Chou
(omit the trailing year when both years are the same)

Description:
  <functional description and design decisions>

Features:
  - <feature 1>
  - <feature 2>

Dependencies:
  - <package>==<version>

Version History:
  x.x.x (YYYY-MM-DD): <change description>
```

### In-file Structure

- 功能區塊之間用 `# ==============================` 分隔線 + 標籤。
- 每個 function/method docstring 必含 `Args` 與 `Returns`。
- 版本號只存在檔案內（`Version` + `Version History`），永不放進檔名。

---

## Core Principles

- **Simplicity First**：每個改動盡可能簡單，影響最少的程式碼。
- **No Laziness**：找根因，不做臨時貼補。
- **Surface Assumptions**：所有假設寫出來，永不沉默猜測。
- **Proactive Warnings**：主動指出風險、邊界情況與副作用，即使沒被問。
